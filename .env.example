# Copy this file to .env and set values as needed

# Kafka UI container name (for docker compose)
KAFKA_UI_CONTAINER_NAME=kafka-ui-app

# Kafka cluster definition (first cluster)
KAFKA_CLUSTERS_0_NAME=cluster-1
KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS=localhost:9092

# Optional: Zookeeper (not required for modern clusters)
# KAFKA_CLUSTERS_0_ZOOKEEPER=localhost:2181

# Optional: Make the UI allow adding clusters at runtime
DYNAMIC_CONFIG_ENABLED=true

# Optional: Read-only mode (disable produce/delete operations)
# KAFKA_CLUSTERS_0_READONLY=true

# Optional: SASL/SSL authentication examples
# KAFKA_CLUSTERS_0_PROPERTIES_SECURITY_PROTOCOL=SASL_SSL
# KAFKA_CLUSTERS_0_PROPERTIES_SASL_MECHANISM=PLAIN
# KAFKA_CLUSTERS_0_PROPERTIES_SASL_JAAS_CONFIG=org.apache.kafka.common.security.plain.PlainLoginModule required username="user" password="pass";

# UI port (container listens here). Railway may set PORT; if so, set SERVER_PORT=$PORT in Railway variables.
SERVER_PORT=8080

# Shared Kafka client settings for producer/consumer
KAFKA_BROKERS=localhost:9092
KAFKA_TOPIC=demo-topic
KAFKA_SSL=false
# KAFKA_SECURITY_PROTOCOL=SASL_SSL
# KAFKA_SASL_MECHANISM=PLAIN
# KAFKA_SASL_USERNAME=
# KAFKA_SASL_PASSWORD=

# Producer service
PRODUCER_CONTAINER_NAME=kafka-producer-app
PRODUCER_KAFKA_CLIENT_ID=producer-app
PRODUCER_INTERVAL_MS=2000

# Consumer service
CONSUMER_CONTAINER_NAME=kafka-consumer-app
CONSUMER_KAFKA_CLIENT_ID=consumer-app
KAFKA_GROUP_ID=demo-group

# All-in-one image settings
ALL_IN_ONE_CONTAINER_NAME=kafka-suite
# Provide a Kafka UI JAR URL (downloaded at runtime by the all-in-one image)
# Example (replace with a valid release URL from provectus/kafka-ui):
# KAFKA_UI_JAR_URL=https://github.com/provectus/kafka-ui/releases/download/v0.X.Y/kafka-ui-api-v0.X.Y.jar
